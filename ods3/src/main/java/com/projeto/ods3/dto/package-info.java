/**
 * Contratos de entrada e saída da API, agrupados por recurso nos subpacotes
 * {@code usuario}, {@code auth}, {@code treino} e {@code comentario}.
 *
 * <p>As entidades/documentos nunca são expostos diretamente como contrato público,
 * e a senha não aparece em nenhum DTO de resposta.
 */
package com.projeto.ods3.dto;
